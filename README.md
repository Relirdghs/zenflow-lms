# ZenFlow — Yoga LMS Platform

Платформа для обучения йоге с **Next.js 16** (App Router), **Supabase**, **Tailwind CSS v4**, **Shadcn UI**, **TanStack Query** и **Framer Motion**. Включает **портал клиента** (`/dashboard`), **админ-панель** (`/admin`) и **супер-админ** (`/admin/super`) в одном приложении.

## Технологический стек

- **Framework:** Next.js 16.1.6 (App Router), TypeScript
- **Стили:** Tailwind CSS v4.1, Shadcn UI
- **База данных и Auth:** Supabase (PostgreSQL, Auth, Storage, RLS)
- **Состояние:** TanStack Query (серверное состояние)
- **UI:** Lucide React, Framer Motion
- **ZenBuilder:** визуальный конструктор уроков с кнопками управления
- **i18n:** поддержка русского, казахского, английского языков

## Быстрый старт

### 1. Установка

```bash
cd my-app
npm install
```

### 2. Настройка Supabase

📖 **Полное руководство:** См. [SUPABASE_SETUP.md](./SUPABASE_SETUP.md) для детальных инструкций на русском языке.

**Кратко:**
1. Создайте проект на [supabase.com](https://supabase.com)
2. В **SQL Editor** выполните миграции по порядку:
   - `supabase/migrations/00001_zenflow_schema.sql` — основная схема
   - `supabase/migrations/00002_add_new_block_types.sql` — новые типы блоков
   - `supabase/migrations/00003_goals_to_groups.sql` — групповые цели
   - `supabase/migrations/00004_allow_client_enrollments.sql` — запись клиентов на курсы и обновление прогресса
   - `supabase/migrations/00005_group_members_and_avatars.sql` — участники групп и аватары
   - `supabase/migrations/00006_course_visibility_and_group_access.sql` — приватные курсы и доступ группам
   - `supabase/migrations/00007_fix_profiles_policy_recursion.sql` — фикс рекурсии в RLS на profiles
3. Отключите подтверждение email (см. SUPABASE_SETUP.md, Шаг 9.5)

### 3. Переменные окружения

```bash
# Скопируйте .env.example в .env.local
cp .env.example .env.local
```

Заполните переменные из Supabase **Settings → API**:
- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- `SUPABASE_SERVICE_ROLE_KEY` (для seed скрипта)
- `SUPER_ADMIN_EMAILS` (опционально, для авто‑назначения супер‑админа)

### 4. Запуск

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) и зарегистрируйтесь.

### 5. Тестовые данные (опционально)

```bash
npm run seed
```

Создаст 4 курса × 4 урока с блоками контента.

---

## Роли пользователей

- **Client (Клиент):** `/dashboard` — просмотр курсов, плеер уроков, цели, чат с администратором
- **Admin (Администратор):** `/admin` — создание курсов, ZenBuilder, управление группами, чат с клиентами
- **Super-Admin (Супер-админ):** `/admin/super` — изменение ролей, просмотр всех чатов, редактирование любых курсов

**Установка роли:** Supabase **Table Editor** → `profiles` → измените `role` на `admin` или `super_admin`.

---

## ZenBuilder (конструктор уроков)

Администраторы создают уроки из блоков:

### Типы блоков

**Структура контента:**
- **H1** — заголовок первого уровня
- **H2** — заголовок второго уровня  
- **Текст** — форматированный текст (Markdown)
- **Важное (Callout)** — цветной блок с иконкой (info, warning, success, error)
- **Ссылка** — кнопка-ссылка с выбором стиля

**Медиа:**
- **Изображение** — загрузка через drag-and-drop с превью
- **Слайдер** — галерея изображений с изменением порядка
- **Видео** — YouTube / Vimeo / прямая ссылка

**Интерактив:**
- **Тест** — вопросы с вариантами ответов и объяснением
- **Чек-лист** — список задач для практики
- **Таймер** — обратный отсчет для статических асан

### Управление

- **Кнопки "Вверх/Вниз"** для перестановки блоков
- **Drag-and-drop** для загрузки изображений
- **Визуальный редактор** с превью
- **Автосохранение** порядка блоков

**Доступ:** Админ → Курсы → [Курс] → Уроки → ZenBuilder

---

## Интернационализация (i18n)

Проект подготовлен для мультиязычности:

- **Текущий язык:** Русский (по умолчанию)
- **Подготовлены словари:** `lib/i18n/dictionaries/` — ru.json, kk.json, en.json
- **Конфигурация:** `lib/i18n/config.ts` — список языков
- **Интерфейс:** Системные сообщения на русском языке

Для активации переключения языков добавьте middleware для определения языка и обновите layout для передачи словарей.

---

## Плеер курса и прогресс

Клиенты открывают курсы из **Панель управления → Мои курсы**. Прогресс сохраняется в `enrollments.progress_percent` и автоматически обновляется.

---

## Чат

- **Панель управления → Чат:** клиенты и администраторы видят свои беседы (Supabase Realtime)
- **Супер-админ:** переключатель **"Все беседы"** для мониторинга (режим только для чтения)

---

## Скрипты

- `npm run dev` — запуск dev сервера
- `npm run build` — production билд
- `npm run start` — запуск production сервера
- `npm run seed` — создание тестовых данных (4 курса × 4 урока)
- `npm run confirm-users` — подтверждение email всех пользователей (для разработки)
- `npm run lint` — ESLint

---

## Структура проекта

```
my-app/
├── app/                    # Маршруты Next.js
│   ├── (auth)/            # Авторизация
│   ├── dashboard/         # Портал клиента
│   ├── admin/             # Админ-панель
│   └── auth/              # Callback и signout
├── components/
│   ├── ui/                # Shadcn UI + ImageUpload
│   ├── course-player/     # Рендереры блоков для плеера
│   └── zen-builder/       # Конструктор уроков
├── lib/
│   ├── supabase/          # Клиенты Supabase
│   ├── i18n/              # Словари и конфигурация
│   └── utils.ts           # Утилиты
├── types/
│   └── database.ts        # Типы для всех блоков
├── supabase/
│   └── migrations/        # SQL миграции
├── scripts/               # Скрипты (seed, confirm-users)
└── middleware.ts          # RBAC
```

---

## Документация

- **[README.md](./README.md)** — основная документация (этот файл)
- **[SUPABASE_SETUP.md](./SUPABASE_SETUP.md)** — пошаговая настройка Supabase на русском
- **[CHANGELOG.md](./CHANGELOG.md)** — история изменений
- **[UPGRADE.md](./UPGRADE.md)** — инструкция по обновлению с версии 1.0

---

## Миграции БД

**Выполните по порядку в SQL Editor:**

1. `00001_zenflow_schema.sql` — основная схема (таблицы, RLS, функция handle_new_user)
2. `00002_add_new_block_types.sql` — новые типы блоков (h1, h2, callout, link)
3. `00003_goals_to_groups.sql` — групповые цели (добавляет group_id, обновляет RLS)
4. `00004_allow_client_enrollments.sql` — запись клиентов на курсы и обновление прогресса
5. `00005_group_members_and_avatars.sql` — участники групп и аватары
6. `00006_course_visibility_and_group_access.sql` — приватные курсы и доступ группам
7. `00007_fix_profiles_policy_recursion.sql` — фикс рекурсии в RLS на profiles

**При обновлении с версии 1.0 или 2.0:** выполните только недостающие миграции.

---

## Особенности

✅ Визуальный конструктор уроков с 11 типами блоков  
✅ Drag-and-drop загрузка изображений  
✅ Кнопки управления порядком блоков (вверх/вниз)  
✅ Русский интерфейс по умолчанию  
✅ Подготовка для казахского и английского языков (i18n)  
✅ RBAC (контроль доступа по ролям: клиент, админ, супер-админ)  
✅ Групповые цели (цели для групп, а не личные)  
✅ Чат в реальном времени с поиском пользователей (Supabase Realtime)  
✅ Отслеживание прогресса обучения  
✅ Минималистичный дизайн Zen (sage green, warm beige, slate)  

---

**Создано с ❤️ для практики йоги и осознанности.**
